定义一个类来表示一个矩形，包括计算矩形的面积和周长的方法：
python
Copy code
class Rectangle:
    def __init__(self, width, height):
        self.width = width
        self.height = height
    
    def area(self):
        return self.width * self.height
    
    def perimeter(self):
        return 2 * (self.width + self.height)

rect = Rectangle(4, 5)
print(rect.area())
print(rect.perimeter())
